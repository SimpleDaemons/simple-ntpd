#!/bin/bash
# Post-removal script for simple-ntpd

set -e

PROJECT_NAME="simple-ntpd"
CONFIG_DIR="/etc/$simple-ntpd"
LOG_DIR="/var/log/$simple-ntpd"

if [ "$1" = "purge" ]; then
    # Remove configuration files (optional - user may want to keep them)
    # Uncomment to remove config on purge:
    # rm -rf "$CONFIG_DIR"
    
    # Remove log files
    rm -rf "$LOG_DIR"
    
    # Remove service user (optional)
    # userdel ntpddev 2>/dev/null || true
fi

exit 0

